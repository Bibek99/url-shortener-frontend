import React from 'react';

const Disclaimer = () => {
    return (
        <div className="disclaimer">
            <div className="dis-text">
                Attention! This is a project running on a test server. When
                deployed on a live production server url will get shorter
                according to the domain served.
            </div>
        </div>
    );
};

export default Disclaimer;
